doctype html
html
  head
    title= title
  body
    h1= title

    if message
      p= message

    form(action='/fileupload/easy/upload' method='post' enctype='multipart/form-data')
      input(type='file' name='file')
      button(type='submit') Upload
